import { DomChangedDirective } from './dom-changed.directive';

describe('DomChangedDirective', () => {
  it('should create an instance', () => {
    const directive = new DomChangedDirective();
    expect(directive).toBeTruthy();
  });
});
